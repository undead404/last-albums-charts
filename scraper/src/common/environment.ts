export const { APP_NAME = 'app' } = process.env;
export const { DISCOGS_ACCESS_TOKEN } = process.env;
export const { LASTFM_API_KEY } = process.env;
export const { ALGOLIA_API_KEY, ALGOLIA_APP_ID } = process.env;
